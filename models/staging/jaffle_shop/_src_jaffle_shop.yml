sources:
  - name: jaffle_shop
    database: raw #Will assume this as target database if not specified
    schema: jaffle_shop #by default assumes schema name as source name so this step is redundant, but still a good practice
    tables:
      - name: customers
      - name: orders
        config:
          freshness:
            warn_after:
              count: 24
              period: hour
            error_after:
              count: 48
              period: hour
          loaded_at_field: _etl_loaded_at