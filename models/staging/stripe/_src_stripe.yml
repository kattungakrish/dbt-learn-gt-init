sources:
  - name: stripe
    database: raw #Will assume this as target database if not specified
    schema: stripe #by default assumes schema name as source name so this step is redundant, but still a good practice
    tables:
      - name: payment
        config:
          freshness:
            warn_after:
              count: 12
              period: hour
            error_after:
              count: 24
              period: hour
          loaded_at_field: _batched_at